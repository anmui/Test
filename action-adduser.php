<?php    require "test.php";    $con=new mysql();    //查看id是否合法    var_dump( $_POST["id"]);    if(strlen($_POST["id"])>10||strlen($_POST["id"])==0)    {        //confirm("用户ID不合法！");        echo "用户ID不合法！";        //header("Location:index.php");        exit();    }    //查看用户id是否已被占用    $sql="select * from test1 where id=".$_POST["id"];    $num=mysqli_num_rows($con->selectTable($sql));    if($num!=0)    {        echo "用户ID已占用！";        //confirm("用户ID已占用！");        //header("Location:index.php");        exit();    }    $sql="insert into test1(name,id) values (\"".$_POST["name"]."\",".$_POST["id"].")";    //$sql="insert into test1(name,id) values (".$_POST("name").$_POST("id").")";    //echo $sql;    $con->insertTable($sql);    header("Location:index.php");?>